#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef double db;
int main()
{
    ll r,x,y,a,b;
    cin>>r>>x>>y>>a>>b;
    cout<<ceil(sqrt(pow(abs(x-a),2)+pow(abs(b-y),2))/(2*r));

}

